projection;
strict ( 2 );
use draft;

define behavior for ZC_APG_Point //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _Gates { create; with draft; }
}

define behavior for ZC_APG_GateHandle //alias <alias_name>
{
  use update;
  use delete;

  use association _Point { with draft; }
}